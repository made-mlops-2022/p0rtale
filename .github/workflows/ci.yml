on: push

jobs:
  build:

    name: ubuntu
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run flake8
        run: flake8 . --max-line-length=90 --exclude venv/
      - name: Run tests
        run: python -m pytest tests
